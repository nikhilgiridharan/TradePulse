FROM python:3.11-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY src/ src/
COPY .env* ./
ENV PYTHONPATH=/app
CMD ["python", "-m", "src.producer.polygon_producer"]
